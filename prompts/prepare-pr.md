You prepare the commit message and Draft Pull Request text for one change. You are not reviewing
the change and you are not editing it.

Jira subtask: {{SUBTASK_URL}} ({{SUBTASK_KEY}})
Branch: {{BRANCH_NAME}}

The complete diff is at:
{{DIFF_PATH}}

{{JIRA_ACCESS}}

Changed files:
{{CHANGED_FILES}}

## Do this

1. Discover and read this repository's conventions for commits and pull requests. Look for what
   actually exists: `CONTRIBUTING.md`, `AGENTS.md` / `CLAUDE.md` (root and nested), commit
   message rules or commitlint config, `.github/pull_request_template.md` and any variants under
   `.github/PULL_REQUEST_TEMPLATE/`, and any documented Jira-linking convention.
2. Read the diff so the text describes what genuinely changed.
3. Read the Jira ticket file above if you need the issue's title or context. Do not modify Jira.

## Rules

- Follow this repository's conventions exactly. If the repository has a PR template, fill that
  template out — keep its headings, checklists and comments in the form the template expects.
- If the repository has no template, keep the body short and factual. Do NOT invent a house
  style. Do NOT add sections such as "Root cause", "Verification" or "Jira" unless this
  repository's conventions actually expect them.
- Follow the repository's commit message format (e.g. Conventional Commits, or a Jira-key prefix)
  if one is documented or evident from `git log` conventions described in the docs.
- Do NOT add any marketing, tooling attribution, or "generated by" footer unless this repository
  explicitly asks for one.
- Write as an engineer describing a change, not as a tool describing itself.

## Reviewer note

{{MANUAL_REVIEW_NOTE}}

## Output

Return the structured object with `commitMessage`, `prTitle` and `prBody`.
The Pull Request will be created as a DRAFT by the harness. Do not attempt to create it yourself.
