Write the commit message and Draft PR text for the change on branch `{{BRANCH_NAME}}`, for Jira
subtask {{SUBTASK_KEY}} ({{SUBTASK_URL}}).

Diff: {{DIFF_PATH}}
The handoff it was built from: {{HANDOFF_PATH}}
Changed files:
{{CHANGED_FILES}}

Follow this repository's commit and PR conventions, and fill out its PR template if it has one. No
tooling attribution or "generated by" footer.

The implementing agent has normally committed already; `commitMessage` is the fallback for
anything it left uncommitted. Return `commitMessage`, `prTitle`, `prBody` — the harness opens the
PR as a draft.
